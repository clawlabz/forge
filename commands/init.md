---
description: Initialize a new Forge project with config and state files
---

# /forge:init — Initialize Forge Project

Initialize a new project with Forge pipeline structure, configuration, and state tracking.

## Usage

```
/forge:init                    # Interactive setup
/forge:init --preset claw      # Use ClawLabz preset
/forge:init --preset generic   # Use generic preset
```

## How It Works

Read the full initialization logic from the `init` skill (`skills/init/SKILL.md`), then execute it.
