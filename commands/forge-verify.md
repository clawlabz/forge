# /forge:verify — P6 Three-Round Verification

Conduct 3 rounds of multi-agent verification with parallel fixes.

## Usage

```
/forge:verify
```

## How It Works

Read the full verification logic from the `verify` skill (`skills/verify/SKILL.md`), then execute it.
