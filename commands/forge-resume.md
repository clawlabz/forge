# /forge:resume — Resume Interrupted Pipeline

Resume a Forge pipeline from the last checkpoint.

## Usage

```
/forge:resume
```

## How It Works

Read the full resume logic from the `resume` skill (`skills/resume/SKILL.md`), then execute it.
