# /forge:develop — P4 Multi-Agent Development

Execute the development plan with multi-agent parallel development and checkpoints.

## Usage

```
/forge:develop
```

## How It Works

Read the full development logic from the `develop` skill (`skills/develop/SKILL.md`), then execute it. Use the Agent tool for parallel execution when ≥3 independent tasks exist.
